""" configuration to use nginx as a reverse proxy
  location / {

     limit_req zone=api_limit;
    proxy_pass http://127.0.0.1:8000;

    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
             }
    


 #configuration for compression(all json response)
 gzip on
 gzip_comp_level 5;
    gzip_min_length 1024;
    gzip_vary on;
    gzip_proxied any;

 gzip_types
        application/json
        application/xml
        text/plain;
      # ( text/css, application/xml+rss text/javascript) can be used in future updates 


#configuration for static file serving 
  Serve uploaded profile pictures
    location /media/ {
        alias C:\Users\Divyanshu\Desktop\DevAtlas\uploads;
    }

    

#configuration for rate limiting 
limit_req_zone $binary_remote_addr zone=api_limit:10m rate=10r/s;

             """ 